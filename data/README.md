# data/

Reserved for future material/sample datasets (e.g. additional stand-in course
material for demos). Currently empty — the Phase 0 seed's stand-in material
(`sample_db_course.txt`) lives in `backend/seed/` because it is loaded by
`seed.py`/`simulate.py` as a package resource, not a standalone data asset.
